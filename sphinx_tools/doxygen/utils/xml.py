from xml.etree.ElementTree import Element
from xml.etree.ElementTree import ElementTree
from typing import List


#def get_compound_name_from_xml(compound_entry) -> str:
    # name_tag = compound_entry.find("name")
    # name = name_tag.contents[0]
    #
    # if name is not str:
    #     raise ValueError(f'Name tag in {compound_entry} is not a string')
    #
    # return name